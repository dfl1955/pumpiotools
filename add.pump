
# This code has not been tested

# INSTALL -------------------------

[ -d /opt/local ] || mkdir /opt/local

cd /opt/local

[ -d pump.io ] || git clone https://github.com/e14n/pump.io.git

cd pump.io

npm install -g jshint
npm install -g
npm test -g

npm install -g databank-mongodb


